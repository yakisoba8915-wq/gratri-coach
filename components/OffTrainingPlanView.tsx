import { CalendarDays,Clock,Info } from "lucide-react";
import type { OffTrainingPlan,TrainingCategory } from "@/lib/types";

const colors:Record<TrainingCategory,string>={"シバカツ":"bg-sky-50 text-sky-600","筋トレ":"bg-orange-50 text-orange-600","柔軟":"bg-emerald-50 text-emerald-600"};
export default function OffTrainingPlanView({plan,onRebuild}:{plan:OffTrainingPlan;onRebuild:()=>void}){
  return <section className="mb-8"><div className="mb-4 rounded-3xl bg-gradient-to-br from-navy to-glacier p-5 text-white shadow-xl shadow-sky-200"><p className="text-xs font-bold text-cyan-100">YOUR OFF-SNOW PLAN</p><h2 className="mt-1 text-xl font-black">{plan.title}</h2><p className="mt-2 text-sm leading-6 text-white/75">{plan.description}</p><div className="mt-4 flex gap-2 text-xs font-bold"><span className="flex items-center gap-1 rounded-full bg-white/15 px-3 py-2"><CalendarDays size={14}/>週{plan.weeklyDays}日</span><span className="flex items-center gap-1 rounded-full bg-white/15 px-3 py-2"><Clock size={14}/>{plan.sessionMinutes}分</span></div></div><div className="space-y-4">{plan.days.map((day)=><article key={day.label} className="card"><p className="text-xs font-black text-glacier">{day.label}</p><h3 className="mt-1 text-lg font-black">{day.theme}</h3><div className="mt-4 space-y-3">{day.exercises.map((item)=><div key={`${day.label}-${item.name}`} className="rounded-2xl bg-slate-50 p-3"><div className="flex items-start justify-between gap-2"><div><span className={`rounded-full px-2 py-1 text-[10px] font-bold ${colors[item.category]}`}>{item.category}</span><p className="mt-2 font-black">{item.name}</p></div><p className="shrink-0 text-xs font-bold text-glacier">{item.prescription}</p></div><p className="mt-2 text-xs text-slate-500">関連能力：{item.ability}</p><p className="mt-1 flex gap-1 text-[11px] text-slate-400"><Info size={12} className="mt-0.5 shrink-0"/>{item.caution}</p></div>)}</div></article>)}</div><button onClick={onRebuild} className="mt-4 w-full rounded-2xl border border-slate-200 bg-white px-5 py-3 text-sm font-bold text-slate-500">プランを作り直す</button></section>;
}
